# Agent for Excel — Design QA

## 对照范围

- 参考设计：`C:\Users\71824\.codex\generated_images\019f65a7-8db1-7c30-881b-676e5e021c94\exec-b8a9b2b6-0ebc-4a03-a51a-1bb47dbf683e.png`
- Excel 实机：首页 `artifacts\agent-modern-home-final.png`
- Excel 实机：设置菜单 `artifacts\agent-modern-settings.png`
- 并排对照：`artifacts\design-qa-comparison.png`
- 对照状态：右侧任务窗格、浅色主题、设置菜单展开；实机截图按参考稿比例归一化后比较。

## 视觉验收

- [x] 品牌只在顶部出现一次，没有重复的机器人或产品名称。
- [x] 折纸机器人使用真实位图资源，功能图标来自系统图标库，无 emoji 或占位符。
- [x] 暖白背景、雾绿色状态、深色正文与橙色辅助图标匹配参考方向。
- [x] 欢迎标题改为任务导向的“今天想分析哪张表？”。
- [x] 四个分析模板保持 2×2 布局，窄窗格下文字不溢出。
- [x] 输入区固定在底部，包含模板入口、示例占位、发送按钮和上下文说明。
- [x] 设置菜单使用分组结构、圆角卡片、轻阴影和明确的当前状态。
- [x] Microsoft YaHei UI 字体在 Excel 高 DPI 环境中显示正常。

## 交互验收

- [x] 设置按钮可展开/收起分组菜单。
- [x] “模型与连接”可打开模型配置窗口，已有模型预设保持可用。
- [x] 首页模板、全部模板和分析方法可把结构化提示带入输入框。
- [x] “工具与权限”如实显示读取、写值、公式、格式、图表和普通数据透视表已开启，以及 Power Query、Power Pivot 和宏尚未实现。
- [x] 模板库弹层可打开、关闭，并可选择模板。
- [x] 左下角加号可打开分析模板库。
- [x] 对话发送、模型标签刷新和写操作前确认逻辑保留。

## 缺陷结论

- P0：0
- P1：0
- P2：0

## 工程验证

- [x] `Debug` 配置编译成功，无编译错误。
- [x] 在真实 Microsoft Excel 中完成首页、设置菜单、模板库和模板回填验证。
- [x] 当前能力边界为“对话 + 单元格读写 + 公式 + 格式 + 图表 + 普通数据透视表”；未把 Power Query、Power Pivot 和宏标记为可用。

结论：通过。

---

## 2026-07-16 对话体验迭代

### 对照范围

- 用户问题截图：`C:\Users\71824\AppData\Local\Temp\codex-clipboard-3a370823-e684-4ed4-abdb-1ea7aa300cd4.png`
- Excel 实机结果：`artifacts\chat-layout-resized.png`
- 状态：GLM 4.7、只读工具调用、窄任务窗格、结构化表格预览。

### 主要问题与修复

- [x] P1：连续出现多个“正在执行所需操作”气泡。改为单个紧凑状态卡，并在工具完成后原位替换。
- [x] P1：读取结果使用竖线拼接，在窄窗格中无法对齐。改为带列标、行号、网格线与横向滚动的表格卡片。
- [x] P1：模型输出一次性出现。改为 SSE 流式文本渲染，并支持被拆分的流式 tool_calls。
- [x] P2：大区域预览没有范围说明。增加工作表、地址、已读取/总行列数和“部分预览”标记。
- [x] P2：普通回复缺少层级。增加标题、列表、粗体、行内代码、代码块与 Markdown 表格渲染。
- [x] P2：模型偶发只返回思考过程、没有最终正文。增加一次自动续写最终答复的恢复机制。

### 视觉与交互验收

- [x] 表格卡片与现有暖白、雾绿设计语言一致。
- [x] 表头与行号固定为视觉索引，数据单元格宽度一致。
- [x] 对话区只展示前 8 行，避免长表格占满任务窗格；完整工具结果仍回传模型。
- [x] 列数超出视口时使用卡片内部横向滚动，不挤压整个对话区。
- [x] 流式等待、工具执行、工具完成和最终回复四种状态可区分。

### 工程验证

- [x] Debug 构建通过。
- [x] SSE 文本增量与分段工具名/参数拼接测试通过。
- [x] 多轮工具结果回传与空回复自动续写测试通过。
- [x] 单元格读写、公式、格式、图表和数据透视表回归通过。
- [x] 结构化表格结果和 Excel 实机渲染通过。

P0：0；P1：0；P2：0。

final result: passed
